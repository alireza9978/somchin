<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.damasahhre.hooftrim">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name=".activities.reports.MoreInfoActivity"></activity>
        <activity android:name=".activities.FarmSelectionActivity" />
        <activity android:name=".activities.DateSelectionActivity" />
        <activity android:name=".activities.menu.ContactActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity
            android:name=".activities.reports.AddReportActivity"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity android:name=".activities.FarmProfileActivity" />
        <activity android:name=".activities.CowProfileActivity" />
        <activity android:name=".activities.tabs.AddLivestockActivity" />
        <activity android:name=".activities.menu.ProfileActivity" />
        <activity android:name=".activities.tabs.home_activites.VisitActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>