<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.damasahhre.hooftrim">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.App">
        <activity android:name=".activities.reports.AddReportActivity"></activity>
        <activity android:name=".activities.LivestockProfileActivity" />
        <activity android:name=".activities.CattelProfileActivity" />
        <activity android:name=".activities.tabs.MarkedActivity" />
        <activity android:name=".activities.tabs.ReportsActivity" />
        <activity android:name=".activities.tabs.AddLivestockActivity" />
        <activity android:name=".activities.tabs.SearchActivity" />
        <activity android:name=".activities.menu.ProfileActivity" />
        <activity android:name=".activities.tabs.home_activites.VisitActivity" />
        <activity android:name=".activities.tabs.HomeActivity" />
        <activity android:name=".activities.LoginActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".activities.SplashActivity">

        </activity>
    </application>

</manifest>